 const os = require('os'); // Importing the os module
 console.log(os.platform()); // prints the platform of the OS
 console.log(os.homedir()); // prints the home directory of the
 console.log(os.freemem()); // prints the freemem of the
 console.log(os.cpus()); // prints the cpus of the